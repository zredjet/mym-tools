[toolchain]
# 最新 stable を追従。
# 経緯: Tauri 2 系の依存 (hashbrown 0.17 等) が edition2024 (Rust 1.85+ で stable) を
# 要求するようになったため、特定パッチでの pin はメンテ負荷が高い。
# Cargo.toml の `rust-version` (MSRV) で実質下限を担保する (現状 "1.88")。
channel = "stable"
components = ["rustfmt", "clippy"]
profile = "minimal"
